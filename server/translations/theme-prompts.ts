/**
 * 主题配置生成提示词
 * 用于指导AI根据用户描述生成符合要求的主题颜色配置
 */

/**
 * 生成主题配置生成提示词
 * 
 * @param colorDescription 用户描述的颜色要求（文字描述）
 * @param mode 主题模式（'light' 或 'dark'）
 * @returns 完整的提示词
 */
export function generateThemeConfigPrompt(
  colorDescription: string,
  mode: "light" | "dark"
): string {
  const modeName = mode === "light" ? "亮色" : "暗色";
  
  return `你是一位专业的UI/UX设计师，擅长设计美观且实用的主题配色方案。请根据用户的描述，生成一个${modeName}主题的完整颜色配置。

## 用户需求

${colorDescription}

## 输出要求

请严格按照以下JSON格式输出主题配置，**只输出JSON对象，不要包含任何markdown代码块标记、说明文字或注释**：

{
  "background": "#颜色值",
  "foreground": "#颜色值",
  "surface": "#颜色值",
  "surfaceMuted": "#颜色值",
  "border": "#颜色值",
  "muted": "#颜色值",
  "primary": {
    "50": "#颜色值",
    "100": "#颜色值",
    "200": "#颜色值",
    "300": "#颜色值",
    "400": "#颜色值",
    "500": "#颜色值",
    "600": "#颜色值",
    "700": "#颜色值",
    "800": "#颜色值",
    "900": "#颜色值",
    "950": "#颜色值"
  },
  "secondary": {
    "50": "#颜色值",
    "100": "#颜色值",
    "200": "#颜色值",
    "300": "#颜色值",
    "400": "#颜色值",
    "500": "#颜色值",
    "600": "#颜色值",
    "700": "#颜色值",
    "800": "#颜色值",
    "900": "#颜色值",
    "950": "#颜色值"
  },
  "accent": {
    "50": "#颜色值",
    "100": "#颜色值",
    "200": "#颜色值",
    "300": "#颜色值",
    "400": "#颜色值",
    "500": "#颜色值",
    "600": "#颜色值",
    "700": "#颜色值",
    "800": "#颜色值",
    "900": "#颜色值",
    "950": "#颜色值"
  }
}

## 设计原则

1. **颜色语义**：
   - \`background\`: 页面主背景色（${mode === "light" ? "通常是白色或浅色" : "通常是深色或黑色"}）
   - \`foreground\`: 主要文字颜色（${mode === "light" ? "深色文字" : "浅色文字"}）
   - \`surface\`: 卡片、面板等表面颜色（${mode === "light" ? "浅色" : "深色"}）
   - \`surfaceMuted\`: 次要表面颜色（比surface更${mode === "light" ? "浅" : "深"}）
   - \`border\`: 边框颜色（${mode === "light" ? "浅灰色" : "深灰色"}）
   - \`muted\`: 次要文字颜色（${mode === "light" ? "中性灰色" : "浅灰色"}）

2. **主色调（primary）**：
   - 根据用户描述确定主色调（如蓝色、绿色、紫色等）
   - 提供完整的色阶（50-950），50最浅，950最深
   - 500通常是主色调的标准色

3. **辅助色（secondary）**：
   - 通常使用中性色（灰色系）或与主色调协调的颜色
   - 用于次要元素、背景等

4. **强调色（accent）**：
   - 用于强调、高亮等特殊场景
   - 可以与主色调形成对比或互补

5. **${modeName}模式特点**：
   ${mode === "light" 
     ? "- 背景色应该明亮，文字应该深色，确保良好的对比度\n   - 主色调应该使用较深的色阶（500-900）\n   - 整体色调应该清新、明亮"
     : "- 背景色应该深色，文字应该浅色，确保良好的对比度\n   - 主色调应该使用较浅的色阶（50-500）\n   - 整体色调应该柔和、护眼"}

6. **颜色值格式**：
   - 所有颜色值必须使用十六进制格式（如 #FFFFFF、#000000）
   - 确保所有颜色值都是有效的十六进制颜色

7. **输出格式**：
   - **只输出JSON对象，不要包含markdown代码块标记**
   - **不要添加任何解释、说明或注释**
   - **确保JSON格式完全正确，可以直接解析**

请根据以上要求，生成符合用户描述的${modeName}主题配置。`;
}

/**
 * 生成主题优化提示词
 * 
 * @param currentColors 当前主题的颜色配置（JSON字符串）
 * @param enhancementDescription 用户描述的优化要求（文字描述）
 * @param mode 主题模式（'light' 或 'dark'）
 * @returns 完整的优化提示词
 */
export function generateThemeEnhancementPrompt(
  currentColors: string,
  enhancementDescription: string,
  mode: "light" | "dark"
): string {
  const modeName = mode === "light" ? "亮色" : "暗色";
  
  return `你是一位专业的UI/UX设计师，擅长优化和调整主题配色方案。请根据用户的优化要求，对现有的${modeName}主题进行优化调整。

## 当前主题配置

${currentColors}

## 用户优化要求

${enhancementDescription}

## 输出要求

请严格按照以下JSON格式输出优化后的主题配置，**只输出JSON对象，不要包含任何markdown代码块标记、说明文字或注释**：

{
  "background": "#颜色值",
  "foreground": "#颜色值",
  "surface": "#颜色值",
  "surfaceMuted": "#颜色值",
  "border": "#颜色值",
  "muted": "#颜色值",
  "primary": {
    "50": "#颜色值",
    "100": "#颜色值",
    "200": "#颜色值",
    "300": "#颜色值",
    "400": "#颜色值",
    "500": "#颜色值",
    "600": "#颜色值",
    "700": "#颜色值",
    "800": "#颜色值",
    "900": "#颜色值",
    "950": "#颜色值"
  },
  "secondary": {
    "50": "#颜色值",
    "100": "#颜色值",
    "200": "#颜色值",
    "300": "#颜色值",
    "400": "#颜色值",
    "500": "#颜色值",
    "600": "#颜色值",
    "700": "#颜色值",
    "800": "#颜色值",
    "900": "#颜色值",
    "950": "#颜色值"
  },
  "accent": {
    "50": "#颜色值",
    "100": "#颜色值",
    "200": "#颜色值",
    "300": "#颜色值",
    "400": "#颜色值",
    "500": "#颜色值",
    "600": "#颜色值",
    "700": "#颜色值",
    "800": "#颜色值",
    "900": "#颜色值",
    "950": "#颜色值"
  }
}

## 优化原则

1. **保持主题模式**：
   - 必须保持${modeName}模式的基本特征
   - ${mode === "light" ? "背景色应该明亮，文字应该深色" : "背景色应该深色，文字应该浅色"}
   - 确保良好的对比度和可读性

2. **基于现有配置优化**：
   - 在现有颜色配置的基础上进行调整
   - 根据用户要求进行有针对性的优化
   - 保持整体风格的一致性

3. **颜色语义**：
   - \`background\`: 页面主背景色
   - \`foreground\`: 主要文字颜色
   - \`surface\`: 卡片、面板等表面颜色
   - \`surfaceMuted\`: 次要表面颜色
   - \`border\`: 边框颜色
   - \`muted\`: 次要文字颜色
   - \`primary\`: 主色调（完整色阶50-950）
   - \`secondary\`: 辅助色（完整色阶50-950）
   - \`accent\`: 强调色（完整色阶50-950）

4. **优化重点**：
   - 根据用户的优化要求，有针对性地调整相关颜色
   - 确保颜色之间的协调性和视觉平衡
   - 保持${modeName}模式的整体风格

5. **颜色值格式**：
   - 所有颜色值必须使用十六进制格式（如 #FFFFFF、#000000）
   - 确保所有颜色值都是有效的十六进制颜色

6. **输出格式**：
   - **只输出JSON对象，不要包含markdown代码块标记**
   - **不要添加任何解释、说明或注释**
   - **确保JSON格式完全正确，可以直接解析**

请根据以上要求，对当前主题进行优化，生成符合用户要求的${modeName}主题配置。`;
}

